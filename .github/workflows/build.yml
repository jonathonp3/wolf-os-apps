name: Build and Release Custom Editor
on:
  push:
    branches: [main]

# Add this block to give the runner permission to create the release
permissions:
  contents: write

jobs:
  flatpak:
    name: "Build and Release"
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/flathub-infra/flatpak-github-actions:gnome-48
      options: -v ${{ github.workspace }}:/workspace --privileged
    steps:
    - uses: actions/checkout@v4
    
    # 1. Install rsync on the Ubuntu Host
    - name: Install rsync
      run: apt-get update && sudo apt-get install -y rsync    

    # 1. Prepare Runtimes
    - name: Install GNOME Runtimes
      run: |
        cd /workspace
        flatpak remote-add --user --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo
        flatpak install --user -y flathub org.gnome.Sdk//50 org.gnome.Platform//50

    # 2. Inject Themes via your Python script
    - name: Inject Themes
      run: python3 update_manifest.py

    # 3. Build STABLE
    - name: Build Stable
      run: |
        flatpak-builder --user --disable-rofiles-fuse --repo=wolf-os-apps--force-clean build-dir-stable org.gnome.TextEditor.json
        flatpak build-update-repo wolf-os-apps
        flatpak build-bundle wolf-os-apps gnome-text-editor-stable.flatpak org.gnome.TextEditor stable

    # 5. Create a GitHub Release and upload the files
    - name: Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: latest-build
        name: "Wolf-OS App Store - Latest"
        body: |
          Automated build of GNOME Text Editor featuring 12 custom themes.
          Primary Theme: Deep Oceanic.
        files: |
          gnome-text-editor-stable.flatpak
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

